interface:
  display_name: "MCU C++ Code Review"
  short_description: "Review STM32F042 C++ IRQ and DMA firmware"
  default_prompt: "Use $mcu-cpp-code-review to review this STM32F042 C++ firmware change for IRQ, DMA, memory, and code-size risks."
